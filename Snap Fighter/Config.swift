import Foundation

enum Config {
    // Replace with your deployed Worker URL, e.g. https://snap-fighter-ai.<subdomain>.workers.dev
    static let workerAnalyzeEndpoint = "https://YOUR_WORKER_URL/analyze"
}
